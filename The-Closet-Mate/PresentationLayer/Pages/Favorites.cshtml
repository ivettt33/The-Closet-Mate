@page
@model PresentationLayer.Pages.FavoritesModel
@{
    ViewData["Title"] = "Your Favorites";
}

<h2>Your Favorite Items</h2>

@if (Model.FavoriteItems.Count == 0)
{
    <p class="text-muted">You haven’t added any favorites yet.</p>
}
else
{
    <div class="row row-cols-2 row-cols-sm-3 row-cols-md-4 g-3">
        @foreach (var item in Model.FavoriteItems)
        {
            <div class="col">
                <div class="card h-100 shadow-sm">
                    @if (!string.IsNullOrEmpty(item.ImagePath))
                    {
                        <img src="@item.ImagePath" alt="@item.Name"
                             class="card-img-top"
                             style="height: 160px; object-fit: contain;" />
                    }
                    else
                    {
                        <div class="d-flex align-items-center justify-content-center bg-light"
                             style="height: 160px; font-size: 0.8rem;">
                            <span class="text-muted">No image</span>
                        </div>
                    }

                    <div class="card-body p-2">
                        <h6 class="card-title mb-1">@item.Name</h6>
                        <p class="mb-2">
                            <small><strong>Category:</strong> @item.Category<br />
                            <strong>Color:</strong> @item.Color<br />
                            <strong>Season:</strong> @item.Season</small>
                        </p>
                    </div>
                </div>
            </div>
        }
    </div>
}
